appId: com.edulemos.cashcontrol
productName: Personal Cash Control
directories:
  output: dist/release
  buildResources: build
files:
  - "src/main/**/*"
  - "src/preload/**/*"
  - "dist/renderer/**/*"
  - "package.json"
win:
  target: nsis
  icon: build/icon.ico # Opcional: crie um ícone futuramente
nsis:
  oneClick: true
  perMachine: false
  allowToChangeInstallationDirectory: false
  shortcutName: "Cash Control"
  artifactName: "${productName} Setup ${version}.${ext}"
publish:
  provider: github
  owner: edulemos # Substitua pelo seu usuário no github
  repo: personal-cash-control
